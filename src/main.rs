mod emulator;
mod ui;

use emulator::Chip8;
use ui::Ui;

const WIDTH: usize = 64;
const HEIGHT: usize = 32;

fn main() {
    // let mut ui = Ui::new();
    //
    // let buffer = vec![true; WIDTH * HEIGHT];
    //
    // ui.draw(&buffer);
    //
    // let chip = Chip8::new();
}
